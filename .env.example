# Get this from `npx convex dev` output or convex.dev dashboard → your project → Settings
CONVEX_URL=https://your-deployment.convex.cloud

# Crawler settings
CRAWL_MAX_PAGES=500
CRAWL_MAX_DEPTH=3
CRAWL_CONCURRENCY=5
CONTENT_MAX_CHARS=8000
